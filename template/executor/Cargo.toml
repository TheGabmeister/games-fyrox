
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
template = { path = "../game", optional = true }

[features]
default = ["template"]
dylib = ["fyrox/dylib"]
